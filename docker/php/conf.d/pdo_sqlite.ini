; PDO SQLite Extension Configuration
; https://www.php.net/manual/en/ref.pdo-sqlite.php
;
; SQLite database driver for PDO.
;
; To disable this extension, comment out the line below.
extension=pdo_sqlite

; PDO SQLite has no PHP ini directives.
; Connection options are set via DSN or PDO attributes.
