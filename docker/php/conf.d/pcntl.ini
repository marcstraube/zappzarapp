; PCNTL Extension Configuration
; https://www.php.net/manual/en/book.pcntl.php
;
; Process control for signal handling and graceful shutdown.
; Required for queue workers (Symfony Messenger, Laravel Horizon).
;
; Note: This extension only works in CLI mode (not web requests).
;
; To disable this extension, comment out the line below.
extension=pcntl
