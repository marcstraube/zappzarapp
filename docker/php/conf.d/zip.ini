; Zip Extension Configuration
; https://www.php.net/manual/en/zip.configuration.php
;
; To disable this extension, comment out the line below.
extension=zip

; Zip has no runtime configuration directives.
; All settings are controlled via ZipArchive methods.
