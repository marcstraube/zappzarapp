; PDO PostgreSQL Extension Configuration
; https://www.php.net/manual/en/ref.pdo-pgsql.php
;
; PostgreSQL database driver for PDO.
;
; To disable this extension, comment out the line below.
extension=pdo_pgsql

; PDO PostgreSQL has no PHP ini directives.
; Connection options are set via DSN or PDO attributes.
