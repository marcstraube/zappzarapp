# Project Context

<!-- zappzarapp-boilerplate-context -->
<!-- This is the zappzarapp boilerplate's own application context. On `make
     setup` in a derived project it is swapped for a clean template so you can
     describe YOUR application. Platform-wide context lives in `zappzarapp.md`. -->

Application-specific context for this project. Describes what THIS app does —
its domain, custom modules, and business rules — as opposed to the platform
itself (see `zappzarapp.md`).

## Application Overview

The zappzarapp boilerplate ships a reference application used to exercise the
platform end to end.

## Custom Modules

- DevDashboard — standalone development dashboard module (own routes,
  controllers, services)

## Business Logic

<!-- Key business rules, workflows, and domain concepts for this app. -->

## External Services

<!-- APIs, third-party services, and integrations this app depends on. -->
