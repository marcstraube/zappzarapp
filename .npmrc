# pnpm Configuration
# https://pnpm.io/npmrc

# Hoist all dependencies to root node_modules for IDE integration
# This ensures autocomplete works for all workspace dependencies
public-hoist-pattern=*

# Auto-install peer dependencies
auto-install-peers=true

# Use strict peer dependencies (warn on mismatches)
strict-peer-dependencies=false
