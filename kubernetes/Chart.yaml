---
apiVersion: v2
name: zappzarapp
description: A Helm chart for deploying the zappzarapp application stack
type: application
# The chart ships inside the application repository and is released together
# with it, so the chart version tracks the application version.
version: 1.0.0
appVersion: "1.0.0"
keywords:
  - php
  - nodejs
  - nginx
  - postgres
  - redis
maintainers:
  - name: zappzarapp
sources:
  - https://github.com/marcstraube/zappzarapp
